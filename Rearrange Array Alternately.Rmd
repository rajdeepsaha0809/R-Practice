n=as.integer(readline(prompt="Enter the number of elements in the array:"))
arr=array(0)
for(i in 1:n){
  arr[i]=as.numeric(readline(prompt="Enter the value:"))
}
b=array(0)
if(n%%2==0){
  for(i in 1:(n/2)){
    b[2*i-1]=max(arr)
    b[2*i]=min(arr)
    arr=arr[arr!=max(arr)&arr!=min(arr)]
  }
}else{
  for(i in 1:((n-1)/2)){
    b[2*i-1]=max(arr)
    b[2*i]=min(arr)
    arr=arr[arr!=max(arr)&arr!=min(arr)]
  }
b=c(b,arr)
}
print(b)

